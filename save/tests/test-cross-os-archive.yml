name: Test cache save with enableCrossOsArchive
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create a file to cache
        run: |
          mkdir -p /tmp/cache-cross-os
          echo "cross-os cache content" > /tmp/cache-cross-os/data.txt

      - id: action
        uses: .
        continue-on-error: true
        with:
          path: /tmp/cache-cross-os
          key: test-cross-os-cache-${{ github.run_id }}
          enableCrossOsArchive: 'true'

      - name: Assert action ran with enableCrossOsArchive (success or graceful failure)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: cache save with enableCrossOsArchive succeeded"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (cache service may not be available under act)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
